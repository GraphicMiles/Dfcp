/**
 * rollingShutterTest.js
 * Future hook for rolling shutter demodulation experiments.
 */

export class RollingShutterTest {
  constructor() {
    this.enabled = false;
  }

  captureRows(imageData, rowStep = 3) {
    // Placeholder: extract row samples for future RS analysis
    return { rows: [], note: 'rolling-shutter not yet implemented' };
  }
}
